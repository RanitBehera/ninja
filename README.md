# NINJA
Python utilities for MP-Gadget BigFile outputs, exposing nested snapshot structures through dot notation with IDE-friendly auto-completion and code intelligence. Also includes reusable boilerplate code and helper utilities for common analysis workflows.

